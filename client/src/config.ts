// API Configuration
export const API_CONFIG = {
  // Use the RPC endpoint for all API calls
  BASE_URL: '/rpc',
  // In production, you would use an environment variable:
  // BASE_URL: import.meta.env.VITE_API_BASE_URL || '/rpc',
  TIMEOUT: 10000,
};
